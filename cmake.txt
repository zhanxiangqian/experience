OPTION(ENABLE_QTITANRIBBON "ENABLE_QTITANRIBBON" ON)
IF(ENABLE_QTITANRIBBON)
  INCLUDE_DIRECTORIES($ENV{QTITANRIBBON_ROOT_DIR}/include)
  ADD_DEFINITIONS("-DENABLE_QTITANRIBBON")
ENDIF()

IF(SALOME_BUILD_GUI)
  LIST(APPEND _other_SOURCES
    CurveCreator_NewSectionDlg.cxx
    CurveCreator_TableView.cxx
    CurveCreator_TreeView.cxx
    CurveCreator_Widget.cxx
  )

SET(_link_LIBRARIES ${QT_LIBRARIES} ${OPENGL_LIBRARIES})
IF(ENABLE_QTITANRIBBON)
  LIST(APPEND _link_LIBRARIES $ENV{QTITANRIBBON_ROOT_DIR}/lib/qtnribbond2.lib)
ENDIF()


IF(ENABLE_QTITANRIBBON)
IF(CMAKE_BUILD_TYPE STREQUAL Debug)
  LIST(APPEND _link_LIBRARIES $ENV{QTITANRIBBON_ROOT_DIR}/lib/qtnribbond2.lib)
ELSE()
  LIST(APPEND _link_LIBRARIES $ENV{QTITANRIBBON_ROOT_DIR}/lib/qtnribbon2.lib)
ENDIF()
ENDIF()
